# Config locale Claude Code (ne pas publier)
.claude/

# Coffre Obsidian (config locale, non versionnée)
.obsidian/

# Artefacts OS / éditeur
Thumbs.db
desktop.ini
.DS_Store

# Azure Functions - config locale (contient la connection string)
api/local.settings.json
api/node_modules/
